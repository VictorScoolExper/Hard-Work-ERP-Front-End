import { useDispatch } from "react-redux";

const usePromise = (reduxAction, values) => {
    const dispatch = useDispatch();

    const response = new Promise((resolve, reject) => {
        
    });
}

export default usePromise;